# Public samples policy

No real customer key photographs are included in the public repository.

Only add:

- synthetic images; or
- explicitly authorized, anonymized images of demonstration/retired keys.

Before adding a sample, strip EXIF metadata and remove customer documents, VINs, number plates, addresses, faces, and other identifying context. Matching bitting should be published only when disclosure is intentional and authorized.
